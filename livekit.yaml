port: 7880
log_level: info

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true

redis:
  address: redis:6379

# API keys are injected at runtime via LIVEKIT_KEYS environment variable:
#   LIVEKIT_KEYS="<api_key>: <api_secret>"
# Do NOT add keys here — this file is safe to commit.

turn:
  enabled: true
  domain: turn.probateaiagent.undo.it
  tls_port: 5349
  udp_port: 3478
